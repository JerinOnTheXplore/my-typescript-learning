// ekhane T = smartWatch er type
// X = bike er type(default = null)
// etar mane developer interface use kore ami nije bole dite parbo 
// smartWatch kon type er hobe
// bike er type ki hobe..

// smart watch er jonno 2 ta alada type banano hoyeche..

interface BrandCharaWatch {
    heartRate: string;
    stopwatch: boolean;
}

interface Applewatch {
    heartRate: string;
    callSupport: boolean;
    caculator: boolean;
    AiFeature: boolean;
}

